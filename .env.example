# Copy to frontend/.env (Create React App only reads REACT_APP_* from that folder)
REACT_APP_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com

# Copy to backend/.env
JWT_SECRET=change-me
